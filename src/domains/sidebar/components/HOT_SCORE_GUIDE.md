# HOT 점수 계산 상세 가이드

## 📊 HOT 점수 알고리즘 설계

### 핵심 공식

```
HOT 점수 = 기본점수 × 시간감쇠

기본점수 = (조회수 × 1) + (좋아요 × 10) + (댓글 × 20)
시간감쇠 = max(0, 1 - (경과시간 / 최대시간))
```

---

## 🎯 가중치 설계 원리

### 1. 조회수 (×1)

**의미**: 수동적 관심도
- 사용자가 클릭만 하면 카운트
- 가장 쉬운 참여 방식
- 기본 인기도 측정

**예시**:
- 조회수 100회 = 점수 +100

### 2. 좋아요 (×10)

**의미**: 적극적 참여
- 글을 읽고 "좋아요" 버튼 클릭 필요
- 조회보다 10배 의미있는 행동
- 콘텐츠 품질 반영

**예시**:
- 좋아요 10개 = 점수 +100 (조회 100회와 동일)

### 3. 댓글 (×20)

**의미**: 최고 수준 참여
- 글을 읽고, 생각하고, 댓글 작성
- 가장 많은 시간과 노력 투자
- 커뮤니티 활성화의 핵심 지표

**예시**:
- 댓글 5개 = 점수 +100 (조회 100회 또는 좋아요 10개와 동일)

---

## ⏰ 시간 감쇠 메커니즘

### 목적

오래된 글이 계속 상위에 머무는 것을 방지하고, **신선한 콘텐츠**를 우선 표시

### 감쇠 계산

```typescript
경과시간(시간) = (현재시간 - 게시글작성시간) / 1시간
최대시간(시간) = 7일 × 24시간 = 168시간

시간감쇠 = max(0, 1 - (경과시간 / 168))
```

### 시간별 감쇠율

| 경과 시간 | 감쇠율 | 실제 점수 비율 |
|----------|-------|--------------|
| 방금 (0시간) | 1.00 | 100% |
| 12시간 | 0.93 | 93% |
| 1일 (24시간) | 0.86 | 86% |
| 2일 (48시간) | 0.71 | 71% |
| 3일 (72시간) | 0.57 | 57% |
| 4일 (96시간) | 0.43 | 43% |
| 5일 (120시간) | 0.29 | 29% |
| 6일 (144시간) | 0.14 | 14% |
| 7일 (168시간) | 0.00 | 0% |

**결론**: 7일이 지나면 아무리 점수가 높아도 HOT 탭에서 사라짐

---

## 🧮 실전 예시

### 예시 1: 신선한 고품질 글

**게시 후 6시간 경과**
- 조회수: 200
- 좋아요: 15
- 댓글: 8

```
기본점수 = (200 × 1) + (15 × 10) + (8 × 20)
        = 200 + 150 + 160
        = 510

시간감쇠 = max(0, 1 - (6 / 168))
        = 1 - 0.036
        = 0.964

HOT점수 = 510 × 0.964 = 491.6
```

### 예시 2: 오래된 고조회 글

**게시 후 5일 경과**
- 조회수: 1000
- 좋아요: 50
- 댓글: 30

```
기본점수 = (1000 × 1) + (50 × 10) + (30 × 20)
        = 1000 + 500 + 600
        = 2100

시간감쇠 = max(0, 1 - (120 / 168))
        = 1 - 0.714
        = 0.286

HOT점수 = 2100 × 0.286 = 600.6
```

**결과**: 예시 1의 신선한 글(491.6점)보다 예시 2의 오래된 글(600.6점)이 여전히 높지만, 기본점수 차이(510 vs 2100)를 고려하면 시간 감쇠가 크게 작용함

### 예시 3: 댓글 폭발 신규 글

**게시 후 2시간 경과**
- 조회수: 150
- 좋아요: 8
- 댓글: 25 (논쟁 발생)

```
기본점수 = (150 × 1) + (8 × 10) + (25 × 20)
        = 150 + 80 + 500
        = 730

시간감쇠 = max(0, 1 - (2 / 168))
        = 1 - 0.012
        = 0.988

HOT점수 = 730 × 0.988 = 721.2
```

**결과**: 댓글이 많으면 조회수가 적어도 HOT 점수가 높음!

---

## 🎚️ 가중치 조정 시나리오

### 시나리오 1: 댓글을 더 중요하게

**목표**: 토론 중심 커뮤니티

```typescript
// 현재
const rawScore = (views * 1) + (likes * 10) + (comments * 20);

// 변경
const rawScore = (views * 1) + (likes * 5) + (comments * 50);
```

**효과**:
- 댓글 1개 = 조회 50회와 동일
- 활발한 토론 글이 상위 노출

### 시나리오 2: 좋아요를 더 중요하게

**목표**: 품질 중심 커뮤니티

```typescript
// 변경
const rawScore = (views * 1) + (likes * 20) + (comments * 15);
```

**효과**:
- 좋아요 1개 = 댓글 1.33개와 비슷
- 양질의 콘텐츠가 상위 노출

### 시나리오 3: 조회수 무시

**목표**: 참여도만 중시

```typescript
// 변경
const rawScore = (likes * 10) + (comments * 20);
```

**효과**:
- 조회수는 HOT 점수에 영향 없음
- 순수 참여도만 평가

---

## 📉 극단적 케이스 분석

### 케이스 1: 조회만 많고 참여 없는 글

- 조회수: 5000
- 좋아요: 2
- 댓글: 0
- 경과: 1일

```
기본점수 = (5000 × 1) + (2 × 10) + (0 × 20) = 5020
시간감쇠 = 0.86
HOT점수 = 5020 × 0.86 = 4317
```

**해석**: 조회만 많아도 HOT 상위 가능

### 케이스 2: 조회 적지만 폭발적 참여

- 조회수: 100
- 좋아요: 50
- 댓글: 40
- 경과: 3시간

```
기본점수 = (100 × 1) + (50 × 10) + (40 × 20) = 1400
시간감쇠 = 0.98
HOT점수 = 1400 × 0.98 = 1372
```

**해석**: 참여도가 높으면 조회가 적어도 HOT 진입 가능

### 케이스 3: 균형잡힌 인기글

- 조회수: 500
- 좋아요: 30
- 댓글: 15
- 경과: 12시간

```
기본점수 = (500 × 1) + (30 × 10) + (15 × 20) = 1100
시간감쇠 = 0.93
HOT점수 = 1100 × 0.93 = 1023
```

**해석**: 가장 이상적인 HOT 게시글

---

## 🔧 실전 튜닝 가이드

### 1. 현재 HOT 점수 분포 확인

```sql
-- 최근 7일 게시글의 HOT 점수 통계
SELECT
  AVG(views + likes*10 + (
    SELECT COUNT(*) FROM comments c
    WHERE c.post_id = p.id
  )*20) as avg_raw_score,
  MAX(views + likes*10 + (
    SELECT COUNT(*) FROM comments c
    WHERE c.post_id = p.id
  )*20) as max_raw_score
FROM posts p
WHERE created_at >= NOW() - INTERVAL '7 days'
  AND is_deleted = false
  AND is_hidden = false;
```

### 2. 가중치 테스트

1. 샘플 게시글 10개 선택
2. 가중치 변경 전/후 HOT 점수 계산
3. 순위 변화 확인
4. 원하는 결과가 나오면 적용

### 3. A/B 테스트

- 사용자 50% → 현재 가중치
- 사용자 50% → 새 가중치
- 클릭률(CTR) 비교

---

## 📊 데이터 기반 개선

### 지표 모니터링

1. **HOT 탭 클릭률**
   - 목표: 20% 이상

2. **HOT 게시글 평균 체류 시간**
   - 목표: 2분 이상

3. **HOT에서 댓글 작성률**
   - 목표: 5% 이상

### 개선 방향

| 지표 | 현재 | 목표 | 조치 |
|-----|-----|-----|-----|
| HOT 클릭률 | 15% | 20% | 가중치 조정 (댓글 ×30) |
| 체류 시간 | 1분 | 2분 | 품질 낮은 글 필터링 |
| 댓글 작성률 | 3% | 5% | 토론 유도형 글 우대 |

---

## 🚨 주의사항

### 1. 악용 방지

**문제**: 자작 조회/좋아요/댓글로 HOT 조작

**해결**:
```typescript
// 최소 기준 설정
if (options?.minScore && options.minScore > 0) {
  filtered = scoredPosts.filter(p => p.hot_score >= options.minScore);
}

// 사용 예시
await getHotPosts({ limit: 20, minScore: 100 });
```

### 2. 시간 감쇠 비활성화 옵션

```typescript
// 특별 이벤트 글은 시간 감쇠 없이
const timeDecay = post.is_pinned ? 1.0 : Math.max(0, 1 - (hoursSince / maxHours));
```

### 3. 게시판별 가중치 차별화

```typescript
// 자유게시판: 댓글 중시
const commentWeight = boardType === 'free' ? 30 : 20;

// 정보게시판: 좋아요 중시
const likeWeight = boardType === 'info' ? 15 : 10;
```

---

**문서 작성일**: 2025-12-02
**최종 업데이트**: 2025-12-02
**버전**: 1.0.0
**작성자**: Claude Code
