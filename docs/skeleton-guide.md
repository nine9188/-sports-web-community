# 스켈레톤 UI 가이드

## 개요

스켈레톤 UI는 콘텐츠 로딩 중 사용자에게 레이아웃 미리보기를 제공하는 플레이스홀더입니다.

## 표준 색상

```
스켈레톤 배경: bg-[#F5F5F5] dark:bg-[#262626]
카드 배경:     bg-white dark:bg-[#1D1D1D]
```

**잘못된 색상 (사용 금지)**
```
❌ bg-[#EAEAEA] dark:bg-[#333333]  (호버 상태용, 스켈레톤에 사용 금지)
```

---

## 스켈레톤 시스템 파일

### 1. 스타일 정의
**`src/shared/styles/skeleton.ts`**
```typescript
export const skeletonBase = 'bg-[#F5F5F5] dark:bg-[#262626] rounded animate-pulse';
export const skeletonText = 'h-4 bg-[#F5F5F5] dark:bg-[#262626] rounded animate-pulse';
export const skeletonCard = 'bg-white dark:bg-[#1D1D1D] border border-black/7 dark:border-0 rounded-lg';
export const skeletonHeader = 'h-12 bg-[#F5F5F5] dark:bg-[#262626] animate-pulse';
```

### 2. 컴포넌트 정의
**`src/shared/components/skeletons/index.tsx`**

| 컴포넌트 | 용도 | Props |
|----------|------|-------|
| `TextSkeleton` | 텍스트 라인 | `width`, `className` |
| `TextBlockSkeleton` | 텍스트 블록 | `lines`, `className` |
| `AvatarSkeleton` | 아바타 | `size` (sm/md/lg) |
| `CardSkeleton` | 카드 컨테이너 | `hasHeader`, `lines` |
| `ListSkeleton` | 리스트 | `count`, `hasAvatar`, `hasHeader` |
| `ListItemSkeleton` | 리스트 아이템 | `hasAvatar` |
| `GridSkeleton` | 그리드 | `count`, `cols` |
| `TableSkeleton` | 테이블 | `rows`, `cols` |
| `ProfileSkeleton` | 프로필 | - |
| `ButtonSkeleton` | 버튼 | `size`, `width` |

---

## 페이지별 스켈레톤 구조

### 1. 게시판 목록 (`/boards/[slug]`)

**실제 UI 구조:**
```
┌─────────────────────────────────┐
│ 게시판 헤더 (팀/리그 정보)        │
├─────────────────────────────────┤
│ 공지사항 (접힘)                  │
├─────────────────────────────────┤
│ 인기글 위젯 (오늘/주간 탭)        │
├─────────────────────────────────┤
│ 호버 메뉴 (탭 형태)              │
├─────────────────────────────────┤
│ 게시글 목록                      │
│ ┌───┬────────────┬────┬────┬──┐│
│ │분류│ 제목        │작성자│날짜│조회││
│ ├───┼────────────┼────┼────┼──┤│
│ │...│ ...        │... │... │..││
│ └───┴────────────┴────┴────┴──┘│
├─────────────────────────────────┤
│ 페이지네이션                     │
└─────────────────────────────────┘
```

**스켈레톤 파일:** `src/app/boards/[slug]/loading.tsx`

**권장 구조:**
```tsx
export default function BoardDetailLoading() {
  return (
    <div className="container mx-auto">
      {/* 게시판 헤더 */}
      <div className={`${skeletonCard} mb-4`}>
        <div className="h-12 px-4 flex items-center bg-[#F5F5F5] dark:bg-[#262626]">
          <div className={`${skeletonText} w-24`} />
        </div>
      </div>

      {/* 인기글 위젯 (선택적) */}
      <div className={`${skeletonCard} mb-4`}>
        <div className="p-4">
          <div className="flex gap-2 mb-3">
            <div className={`${skeletonBase} w-16 h-8 rounded-full`} />
            <div className={`${skeletonBase} w-16 h-8 rounded-full`} />
          </div>
          <div className="space-y-2">
            {Array(3).fill(0).map((_, i) => (
              <div key={i} className={`${skeletonText} w-full`} />
            ))}
          </div>
        </div>
      </div>

      {/* 게시글 목록 */}
      <div className={skeletonCard}>
        <div className={skeletonDivider}>
          {Array(15).fill(0).map((_, i) => (
            <div key={i} className="px-4 py-3 flex items-center gap-3">
              <div className={`${skeletonText} w-14`} />
              <div className={`${skeletonText} flex-1`} />
              <div className={`${skeletonText} w-16 hidden sm:block`} />
              <div className={`${skeletonText} w-10`} />
            </div>
          ))}
        </div>
      </div>

      {/* 페이지네이션 */}
      <div className="flex justify-center gap-2 mt-4">
        {Array(5).fill(0).map((_, i) => (
          <div key={i} className={`${skeletonBase} w-10 h-10`} />
        ))}
      </div>
    </div>
  );
}
```

---

### 2. 게시글 상세 (`/boards/[slug]/[postNumber]`)

**실제 UI 구조:**
```
┌─────────────────────────────────┐
│ 브레드크럼                       │
├─────────────────────────────────┤
│ 게시글 헤더                      │
│ - 제목                          │
│ - 작성자 | 날짜 | 조회수          │
├─────────────────────────────────┤
│ 게시글 본문                      │
│                                 │
│                                 │
├─────────────────────────────────┤
│ 좋아요/싫어요 버튼               │
├─────────────────────────────────┤
│ 댓글 섹션                       │
├─────────────────────────────────┤
│ 관련 게시글                      │
└─────────────────────────────────┘
```

**권장 구조:**
```tsx
export default function PostDetailLoading() {
  return (
    <div className="container mx-auto">
      {/* 브레드크럼 */}
      <div className="flex gap-2 mb-4">
        <div className={`${skeletonText} w-16`} />
        <div className={`${skeletonText} w-20`} />
      </div>

      {/* 게시글 카드 */}
      <div className={skeletonCard}>
        {/* 헤더 */}
        <div className="p-4 border-b border-black/5 dark:border-white/10">
          <div className={`${skeletonText} w-3/4 h-6 mb-3`} />
          <div className="flex gap-4">
            <div className={`${skeletonText} w-20`} />
            <div className={`${skeletonText} w-24`} />
            <div className={`${skeletonText} w-16`} />
          </div>
        </div>

        {/* 본문 */}
        <div className="p-4">
          <div className="space-y-3">
            {Array(8).fill(0).map((_, i) => (
              <div key={i} className={`${skeletonText} ${i % 3 === 0 ? 'w-full' : i % 3 === 1 ? 'w-5/6' : 'w-4/5'}`} />
            ))}
          </div>
        </div>

        {/* 좋아요/싫어요 */}
        <div className="flex justify-center gap-4 py-4 border-t border-black/5 dark:border-white/10">
          <div className={`${skeletonBase} w-20 h-10 rounded-lg`} />
          <div className={`${skeletonBase} w-20 h-10 rounded-lg`} />
        </div>
      </div>

      {/* 댓글 섹션 */}
      <div className={`${skeletonCard} mt-4`}>
        <div className={skeletonHeader} />
        <div className="p-4 space-y-4">
          {Array(3).fill(0).map((_, i) => (
            <div key={i} className="flex gap-3">
              <div className={`${skeletonBase} w-10 h-10 rounded-full`} />
              <div className="flex-1 space-y-2">
                <div className={`${skeletonText} w-24`} />
                <div className={`${skeletonText} w-full`} />
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}
```

---

### 3. 경기 상세 (`/livescore/football/match/[id]`)

**실제 UI 구조:**
```
┌─────────────────────────────────┐
│ 경기 헤더                        │
│ ┌─────┐   2 - 1   ┌─────┐      │
│ │로고 │    vs     │로고 │      │
│ │홈팀 │           │원정팀│      │
│ └─────┘           └─────┘      │
├─────────────────────────────────┤
│ 탭 네비게이션                    │
│ [개요] [라인업] [통계] [순위]     │
├─────────────────────────────────┤
│ 탭 콘텐츠                        │
└─────────────────────────────────┘
```

**스켈레톤 파일:** `src/domains/livescore/components/common/HeadersUI.tsx`

**현재 `MatchHeaderSkeleton` 사용 중 (수정 완료)**

---

### 4. 라이브스코어 메인 (`/livescore`)

**실제 UI 구조:**
```
┌─────────────────────────────────┐
│ 날짜 네비게이션                  │
│ [< ] [1/18] [1/19] [1/20] [>]   │
├─────────────────────────────────┤
│ 리그별 경기 목록                 │
│ ┌───────────────────────────┐   │
│ │ 🏆 프리미어리그            │   │
│ ├───────────────────────────┤   │
│ │ 맨유 2-1 첼시   15:00     │   │
│ │ 리버풀 0-0 아스날  17:30   │   │
│ └───────────────────────────┘   │
└─────────────────────────────────┘
```

**스켈레톤 파일:** `src/app/livescore/loading.tsx`

**권장 구조:**
```tsx
export default function LivescoreLoading() {
  return (
    <div className="container mx-auto">
      {/* 날짜 네비게이션 */}
      <div className={`${skeletonCard} mb-4`}>
        <div className="h-14 flex items-center justify-center gap-2 px-4">
          {Array(7).fill(0).map((_, i) => (
            <div key={i} className={`${skeletonBase} w-14 h-10`} />
          ))}
        </div>
      </div>

      {/* 리그별 경기 목록 */}
      {Array(3).fill(0).map((_, leagueIdx) => (
        <div key={leagueIdx} className={`${skeletonCard} mb-4`}>
          {/* 리그 헤더 */}
          <div className="h-12 px-4 flex items-center gap-2 bg-[#F5F5F5] dark:bg-[#262626]">
            <div className={`${skeletonBase} w-6 h-6 rounded-full`} />
            <div className={`${skeletonText} w-32`} />
          </div>
          {/* 경기 목록 */}
          <div className={skeletonDivider}>
            {Array(3).fill(0).map((_, matchIdx) => (
              <div key={matchIdx} className="px-4 py-3 flex items-center">
                <div className={`${skeletonText} w-12`} />
                <div className="flex-1 flex items-center justify-center gap-2">
                  <div className={`${skeletonText} w-24`} />
                  <div className={`${skeletonBase} w-6 h-6 rounded-full`} />
                  <div className={`${skeletonText} w-12`} />
                  <div className={`${skeletonBase} w-6 h-6 rounded-full`} />
                  <div className={`${skeletonText} w-24`} />
                </div>
                <div className={`${skeletonText} w-12`} />
              </div>
            ))}
          </div>
        </div>
      ))}
    </div>
  );
}
```

---

### 5. 설정 페이지 (`/settings`)

**실제 UI 구조:**
```
┌─────────────────────────────────┐
│ 설정 헤더                        │
├─────────────────────────────────┤
│ 메뉴 목록                        │
│ ┌─────────────────────────────┐ │
│ │ 🔵 프로필 설정              > │ │
│ ├─────────────────────────────┤ │
│ │ 🔒 비밀번호 변경            > │ │
│ ├─────────────────────────────┤ │
│ │ ...                         │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

**스켈레톤 파일:** `src/app/settings/loading.tsx` (이미 올바름)

---

### 6. 상점 페이지 (`/shop`)

**실제 UI 구조:**
```
┌─────────────────────────────────┐
│ 상점 헤더 (포인트 표시)          │
├─────────────────────────────────┤
│ 카테고리 탭                      │
│ [전체] [아이콘] [배경] [효과]     │
├─────────────────────────────────┤
│ 아이템 그리드                    │
│ ┌───┐ ┌───┐ ┌───┐ ┌───┐       │
│ │   │ │   │ │   │ │   │       │
│ │100│ │200│ │150│ │300│       │
│ └───┘ └───┘ └───┘ └───┘       │
└─────────────────────────────────┘
```

**스켈레톤 파일:** `src/app/shop/loading.tsx` (이미 올바름)

---

## 스켈레톤 작성 원칙

### 1. 실제 UI와 구조 일치
```
❌ 단순히 막대만 나열
✅ 실제 레이아웃 구조 반영
```

### 2. 적절한 높이/너비
```tsx
// 제목
<div className={`${skeletonText} w-3/4 h-6`} />

// 본문 텍스트
<div className={`${skeletonText} w-full h-4`} />

// 메타 정보 (작성자, 날짜 등)
<div className={`${skeletonText} w-20 h-3`} />
```

### 3. 반응형 고려
```tsx
// 모바일에서 숨김
<div className={`${skeletonText} w-20 hidden sm:block`} />

// 데스크탑에서 다른 크기
<div className={`${skeletonText} w-full md:w-1/2`} />
```

### 4. 적절한 개수
```tsx
// 목록: 실제 표시되는 개수와 유사하게
{Array(15).fill(0).map(...)}  // 게시글 목록

// 그리드: 한 화면에 보이는 개수
{Array(12).fill(0).map(...)}  // 상점 아이템
```

---

## 체크리스트

스켈레톤 작성 시 확인:

- [ ] 배경색이 `#F5F5F5` / `#262626` 인가?
- [ ] 다크모드 지원 (`dark:` prefix)
- [ ] `animate-pulse` 적용
- [ ] `rounded` 적용
- [ ] 실제 UI 구조와 유사한가?
- [ ] 반응형 처리 (sm/md/lg breakpoints)

---

## 변경 이력

| 날짜 | 작업 | 파일 |
|------|------|------|
| 2026-01-20 | 스켈레톤 색상 통일 | HeadersUI.tsx, Sidebar.tsx, ClientHoverMenu.tsx, ServerPostListWrapper.tsx, ServerPostList.tsx, LiveScoreView.tsx, LeagueStandings.tsx |
| 2026-01-20 | 다크모드 배경 추가 | HeadersUI.tsx (Match/Player/TeamHeaderSkeleton) |
| 2026-01-20 | 문서 생성 | docs/skeleton-guide.md |
